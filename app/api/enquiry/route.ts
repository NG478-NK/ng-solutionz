import {NextRequest,NextResponse} from 'next/server';
import {createClient} from '@supabase/supabase-js';
export async function POST(req:NextRequest){try{const form=await req.formData();const data=Object.fromEntries(form.entries());const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)return NextResponse.json({ok:true,mode:'demo'});const supabase=createClient(url,key);const {error}=await supabase.from('enquiries').insert({name:String(data.name||''),company:String(data.company||''),phone:String(data.phone||''),email:String(data.email||''),service:String(data.service||''),pages:String(data.pages||''),message:String(data.message||''),status:'new'});if(error)throw error;return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:'Unable to submit enquiry'},{status:500})}}
